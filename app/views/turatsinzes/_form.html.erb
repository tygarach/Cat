<%= form_with(model: turatsinze, local: true) do |form| %>
  <% if turatsinze.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(turatsinze.errors.count, "error") %> prohibited this turatsinze from being saved:</h2>

      <ul>
      <% turatsinze.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :firstname %>
    <%= form.text_field :firstname %>
  </div>

  <div class="field">
    <%= form.label :lastname %>
    <%= form.text_field :lastname %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
